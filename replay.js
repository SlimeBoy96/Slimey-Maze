// Function to reload the page
function reloadPage() {
    location.reload();
}

// Event listener for the replay button
document.getElementById('replayButton').addEventListener('click', reloadPage);

